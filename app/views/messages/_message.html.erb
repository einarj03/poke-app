<div class="message <%= message.from?(current_user) ? "right" : "" %>">
  <%= cl_image_tag message.user.photo, class: "avatar-img sm-avatar" %>
  <div class="message-content"><%= message.content %></div>
</div>
